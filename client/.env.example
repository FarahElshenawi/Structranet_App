# Frontend environment variables
# Copy this file to .env and adjust as needed

# API base URL — use /api in development (Vite proxy forwards to backend)
# In production, set this to your deployed backend URL (e.g. https://api.structuranet.com/api)
VITE_API_URL=/api
