{
  "name": "structuranet-ai-server",
  "version": "1.0.0",
  "description": "StructuraNet AI — Node.js server modules (Express middleware, not a standalone server)",
  "private": true,
  "dependencies": {
    "openai": "^4.73.0"
  },
  "peerDependencies": {
    "express": "^4.18.0 || ^5.0.0"
  }
}
